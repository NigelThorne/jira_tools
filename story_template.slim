doctype html
html
  head
    title Slim Examples
    meta name="keywords" content="template language"
    meta name="author" content="nigel"
    link href='https://fonts.googleapis.com/css?family=Bangers' rel='stylesheet' type='text/css'
    link href='https://fonts.googleapis.com/css?family=Nunito' rel='stylesheet' type='text/css'
    link href="https://fonts.googleapis.com/css?family=Russo+One" rel="stylesheet"
    
    script src="https://cdn.jsdelivr.net/jdenticon/1.3.2/jdenticon.min.js"
    script src="https://cdn.jsdelivr.net/js-md5/0.3.0/md5.min.js"

    css:
      body { background-color: white; }
      .story {
            display: block;
            float: left; 
            height: 210px;
            width: 370px;
            border: 1px solid black;
            padding-left: 1em;
            background-color: #8fff96;
            page-break-inside: avoid;
      }
      h1   {
              color: #00c12b;  
              font-family: 'Russo One', sans-serif;
              font-size: 20pt;
              margin-bottom: 0;
              margin-top: 10px;
              margin-right: 10px;
              float:right;
           }
      h2   {
              color: black;  
              font-family: 'Nunito', sans-serif;
              font-size: 26pt;
              margin-top: 0px;
              overflow: hidden;
              //white-space: nowrap; /* Don't forget this one */
              text-overflow: ellipsis;
              float:left;
           }
      svg {
            background-color: white;
            margin-right: 7px;
            position: relative;
            top: 0;
            left: 0;
      }

  body
    #content
      - @index = 0
      - @cfrom = '#8fff96'
      - @cto = '#968fff'
      - @issues.each.with_index do |issue, index|
        - @bgcol = gradiate_color(@cfrom, @cto, index.to_f/@issues.count)
        div.story style="background-color: #{@bgcol};"
          svg width="60" height="60" data-jdenticon-hash="#{Digest::MD5.hexdigest( issue.key )}"
          h1 style="color: #{darken_color(@bgcol)};"
            = issue.key
          h2 = issue.summary

          